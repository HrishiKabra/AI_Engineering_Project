# --- web / serving ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
sse-starlette==2.1.3
pydantic==2.10.4
pydantic-settings==2.7.1

# --- db ---
psycopg[binary,pool]==3.2.3
pgvector==0.3.6

# --- llm / agent ---
openai==1.59.6
langgraph==0.2.62
tiktoken==0.8.0

# --- ingestion ---
pymupdf==1.25.1

# --- dev / test (kept here so the api image can run pytest in CI) ---
pytest==8.3.4
ruff==0.8.6
httpx==0.28.1

# --- eval (optional-heavy deps are imported lazily) ---
numpy==2.2.1
